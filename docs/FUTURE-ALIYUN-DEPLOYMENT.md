# 未来阿里云 ECS 部署说明

网站未来计划部署至阿里云 ECS。当前项目优先采用 Next.js 静态构建，`npm run build` 成功后静态文件位于 `out/`，后续可考虑使用 Nginx 提供静态文件服务。

正式部署前需要准备：

- 已解析的网站域名；
- 有效 SSL 证书；
- ICP 备案信息和公安备案信息；
- ECS 访问权限与经审核的服务器安全策略；
- 对静态路由刷新、404 和缓存策略的部署配置。

正式部署将在网站设计和功能验收后单独进行。本阶段没有连接服务器，没有执行 SSH，没有修改 Nginx、安全组或 SSL 配置，也没有创建自动部署脚本。
